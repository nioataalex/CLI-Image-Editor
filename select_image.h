//swap the values
//if it's required
void swap_values(int *x, int *y);

//selects the coordinates of the image
void select_image(photo *image);

//check if a number was read from
//stdin or another character
int check_is_number(char *s);

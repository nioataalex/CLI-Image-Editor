//saves the image as a binary or ascii
//file, that depends if the word
//"ascii" is read from stdin
void save_image(photo *image);

//cleans the memory after all the operations
//are done
void exit_image(photo *image);

//saves the coordinates of the image
void save_coordinates(photo *image);
